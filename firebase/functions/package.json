{
  "name": "elevenspark-functions",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^4.8.0",
    "next": "14.1.4"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
